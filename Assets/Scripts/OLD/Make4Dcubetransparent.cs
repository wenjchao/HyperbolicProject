using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Make4Dcubetransparent : MonoBehaviour
{
    public int index;
    public GameObject controller;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //if( controller.Getcomponent<FourDObject>().Display[index]==true ) ;
        //else; 
    }
}
